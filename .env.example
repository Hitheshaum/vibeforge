# AWS Control Plane Configuration
# This is YOUR AWS account ID where the control plane runs (can be same as target account for self-hosted)
CONTROL_PLANE_ACCOUNT_ID=123456789012

# IAM Role Configuration
ROLE_NAME=VibeDeployerRole

# Default AWS Region
DEFAULT_REGION=us-east-1

# Bedrock Configuration (must be a Bedrock-supported region)
# Supported regions: us-east-1, us-west-2, ap-southeast-1, ap-northeast-1, eu-central-1, eu-west-3
BEDROCK_REGION=us-east-1

# Bedrock Model ID
# Free/No approval needed:
# - amazon.titan-text-express-v1 (fast, no approval needed)
# - amazon.titan-text-lite-v1 (lighter version)
# - amazon.titan-text-premier-v1:0 (most capable Titan)
# Qwen3 Coder (optimized for code generation):
# - qwen.qwen3-coder-30b-a3b-v1:0 (recommended for coding - 30B model)
# - qwen.qwen3-coder-480b-a35b-v1:0 (larger 480B model, fewer regions)
# May require use case submission (Anthropic):
# - anthropic.claude-3-5-sonnet-20241022-v2:0 (best quality)
# - anthropic.claude-3-haiku-20240307-v1:0 (faster, cheaper)
BEDROCK_MODEL_ID=qwen.qwen3-coder-30b-a3b-v1:0

# UI Configuration
NEXT_PUBLIC_API_BASE=http://localhost:4000

# AWS Credentials (for control plane to assume cross-account role)
# Get these from AWS Console → Security Credentials → Access Keys
# Or run: aws configure get aws_access_key_id
AWS_ACCESS_KEY_ID=your-access-key-here
AWS_SECRET_ACCESS_KEY=your-secret-key-here
