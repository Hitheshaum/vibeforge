# <%= appName %>

Generated by AWS Vibe on <%= new Date().toISOString() %>

## Architecture

- **Blueprint**: <%= spec.blueprint %>
- **Environment**: Dev + Prod stacks
- **Frontend**: Next.js on S3 + CloudFront
- **Backend**: API Gateway + Lambda functions
- **Database**: DynamoDB tables
<% if (spec.auth) { %>- **Auth**: Cognito User Pool<% } %>

## Structure

```
.
├── infra/          # CDK infrastructure
├── web/            # Next.js frontend
├── api/            # Lambda handlers
└── tests/          # Tests
```

## Local Development

```bash
# Install dependencies
cd web && npm install
cd ../api && npm install
cd ../infra && npm install

# Run web locally
cd web && npm run dev

# Deploy infrastructure
cd infra
npm run cdk deploy <%= appName %>-Dev
```

## Deployment

Infrastructure is managed via AWS CDK and deployed automatically by AWS Vibe.

- **Dev Stack**: `<%= appName %>-Dev`
- **Prod Stack**: `<%= appName %>-Prod`

## API Endpoints

<% spec.api.forEach(function(endpoint) { %>
- `<%= endpoint.method %> <%= endpoint.path %>` - <%= endpoint.description || endpoint.handler %>
<% }); %>

## Data Models

<% spec.dataModel.forEach(function(model) { %>
### <%= model.table %>

- Partition Key: `<%= model.partitionKey %>`
<% if (model.sortKey) { %>- Sort Key: `<%= model.sortKey %>`<% } %>
- Attributes: <%= model.attributes.map(a => a.name).join(', ') %>
<% }); %>

## License

MIT
